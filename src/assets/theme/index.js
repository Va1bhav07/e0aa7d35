import { createTheme } from "@mui/material/styles";

export let theme = createTheme({
  palette: {
    airCall: {
      green: "#3dbc4e",
      yellow: "#fdbe41",
      red: "#fc615c",
      greyPrime: "#fbfbfa",
    },
  },
});

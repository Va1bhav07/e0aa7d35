import { createTheme } from "@mui/material/styles";

export let theme = createTheme({
  palette: {
    airCall: {
      // main: "#fbfbfa",
      green: "#3dbc4e",
      yellow: "#fdbe41",
      red: "#fc615c",
      greyPrime: "#fbfbfa",
      // greySecond:''
    },
  },
});
